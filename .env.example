# Environment variable template for Legal Search MVP
#
# Setup instructions:
#   1. Copy this file to .env:   cp .env.example .env
#   2. Replace each placeholder value with your real key.
#   3. Never commit .env — it is listed in .gitignore.

# Supabase
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-public-key

# Database connection string (Supabase → Settings → Database → URI)
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
DATABASE_URL_DIRECT=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# OpenAI API Key (for embeddings)
OPENAI_API_KEY=your-openai-api-key

# Anthropic API Key (for Claude chat)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Application Settings
RATE_LIMIT_PER_HOUR=20
LOG_LEVEL=INFO
