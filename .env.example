# Database Connection
# For Supabase: Use the connection string from Settings > Database > Connection string > URI
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres:Garpursas12@db.niztfucztuqdzfohyuda.supabase.co:5432/postgres
# Optional: Separate pool for direct connections (bypasses connection pooler)
# Use this for migrations and admin tasks
DATABASE_URL_DIRECT=postgresql://postgres:password@localhost:5432/legal_search

# OpenAI API Key (for embeddings)
OPENAI_API_KEY=sk-...

# Anthropic API Key (for Claude chat)
ANTHROPIC_API_KEY=sk-ant-...

# Application Settings
RATE_LIMIT_PER_HOUR=20
LOG_LEVEL=INFO

# Optional: Supabase (if using Supabase client SDK)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
